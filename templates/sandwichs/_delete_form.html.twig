<form method="post" action="{{ path('app_sandwichs_delete', {'id': sandwich.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sandwich.id) }}">
    <button class="btn">Delete</button>
</form>
