<form method="post" action="{{ path('app_texmexs_delete', {'id': texmex.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ texmex.id) }}">
    <button class="btn">Delete</button>
</form>
