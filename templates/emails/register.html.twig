<h1>Activez votre compte</h1>
<p>Bonjour {{ user.email }},</p>
<p>Votre inscription sur le site lagrilladerie est à valider en cliquant sur le lien ci-dessous :</p>
<p><a class='btn btn-primary' href="{{ absolute_url(path('verify_user', {token: token})) }}">{{ absolute_url(path('verify_user', {token: token})) }}</a></p>
<p>Ce lien expirera dans 3 heures</p>
